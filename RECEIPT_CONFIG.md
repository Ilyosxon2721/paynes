# Настройка квитанций (Receipts Configuration)

## Обзор

Квитанции в системе Paynes настроены согласно стандартам A4 с размерами:
- **Ширина**: полная ширина A4 (210mm = ~794px)
- **Высота**: четверть высоты A4 (74mm = ~280px)

## Структура

### 1. Компонент PaymentReceipt.vue

Расположение: `resources/js/components/PaymentReceipt.vue`

Основной компонент квитанции с двухколоночным дизайном:

#### Левая колонка (200px):
- Название "Хабарнома"
- Название компании
- Тип оплаты (Наличный/Безналичный)
- Дата
- Время
- QR-код (опционально)
- Номер платежа
- ФИО кассира

#### Правая колонка:
- Заголовок (тип платежа)
- Детали платежа:
  - Банк
  - Номер счета
  - МФО
  - ИНН
  - Лицевой счет
- ФИО плательщика
- Назначение платежа
- Суммы:
  - Сумма платежа
  - Комиссия
  - Итого

### 2. Размеры квитанции

#### Для экрана:
```css
.receipt-container {
  width: 100%;
  max-width: 794px;  /* A4 width at 96 DPI */
  height: 280px;     /* Quarter A4 height */
  border: 1px solid #000;
}
```

#### Для печати:
```css
@page {
  size: A4;
  margin: 15mm;
}

body {
  width: 180mm;   /* 210mm - 30mm (margins) */
  height: 67mm;    /* Quarter A4 height */
}

.receipt-container {
  width: 180mm;
  height: 67mm;
}
```

### 3. Стандартные отступы

- **Внешние отступы страницы**: 15mm со всех сторон
- **Внутренние отступы квитанции**: 10mm
- **Левая колонка**: 15px padding
- **Правая колонка**: варьируется по секциям (10-15px)

## Использование

### В компоненте Vue:

```vue
<template>
  <PaymentReceipt
    :payment="paymentData"
    company-name="Paynes Kassa"
    :show-q-r-code="false"
  />
</template>

<script setup>
import PaymentReceipt from '@/components/PaymentReceipt.vue';

const paymentData = ref({
  payment_number: '123456',
  created_at: '2026-01-03T10:30:00',
  payment_method: 'cash',
  payer_name: 'Иванов Иван Иванович',
  amount: 10000,
  commission_amount: 100,
  total_amount: 10100,
  currency: 'UZS',
  purpose: 'Оплата коммунальных услуг',
  list_number: '123456789',
  payment_type: {
    name: 'Коммунальные платежи',
    bank_name: 'Узнацбанк',
    account_number: '12345678901234567890',
    mfo: '00123',
    inn: '123456789'
  },
  cashier: {
    full_name: 'Петров П.П.'
  }
});
</script>
```

### Печать квитанции:

Функция `printReceipt()` в `Create.vue` автоматически создает правильно отформатированную HTML-страницу с:
- Правильными размерами A4
- Стилями для печати
- Всеми данными платежа

## Шрифты

Используемые шрифты (в порядке приоритета):
1. Verdana (основной)
2. Arial (резервный)
3. sans-serif (системный)

## Особенности дизайна

### Из старого проекта (сохранено):
- Двухколоночная структура
- Левая узкая колонка с основной информацией
- Правая широкая колонка с деталями
- Разделители между секциями
- Поля с подчеркиванием для данных
- Табличная секция для сумм

### Новые улучшения:
- Адаптивные размеры для печати
- Правильные единицы измерения (mm, pt)
- Flexbox layout для лучшей структуры
- Поддержка опциональных полей
- Интеграция с Vue 3

## Файлы

1. **Компонент квитанции**: `resources/js/components/PaymentReceipt.vue`
2. **Использование в Create**: `resources/js/views/Payments/Create.vue`
3. **Старые примеры**:
   - `docs/cabinet/cabinet/payment/chek.php`
   - `docs/cabinet/cabinet/payment/chek_comunal.php`

## Настройка

### Изменение размеров:

Для изменения размеров отредактируйте константы в `PaymentReceipt.vue`:

```css
.receipt-container {
  max-width: 794px;  /* изменить ширину */
  height: 280px;     /* изменить высоту (текущая - четверть A4) */
}
```

### Изменение названия компании:

```vue
<PaymentReceipt
  company-name="Ваше название"
  :payment="paymentData"
/>
```

### Включение QR-кода:

```vue
<PaymentReceipt
  :show-q-r-code="true"
  :payment="paymentData"
/>
```

## Печать дубликата квитанции

### Функционал в списке платежей

В списке платежей (`resources/js/views/Payments/Index.vue`) добавлена кнопка печати дубликата:

1. **Кнопка печати** (🖨️) - первая в списке действий для каждого платежа
2. При нажатии:
   - Загружаются полные данные платежа с сервера
   - Открывается модальное окно с предпросмотром квитанции
   - Доступна кнопка "Печать дубликата"

### Отличия дубликата от оригинала

Дубликат квитанции имеет следующие отличия:

1. **Водяной знак "ДУБЛИКАТ"**:
   - Полупрозрачная красная надпись (opacity: 0.15)
   - Повернута на 45 градусов
   - Размещена по центру квитанции
   - Размер шрифта: 32pt

```css
.duplicate-watermark {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) rotate(-45deg);
  font-size: 32pt;
  font-weight: bold;
  color: rgba(255, 0, 0, 0.15);
  z-index: 10;
  pointer-events: none;
}
```

### Модальное окно предпросмотра

- **Заголовок**: "Дубликат квитанции"
- **Тело**: Компонент `PaymentReceipt` с полными данными платежа
- **Кнопки**:
  - "Печать дубликата" - открывает диалог печати
  - "Закрыть" - закрывает модальное окно

### API Endpoints

Функция печати дубликата использует endpoint:

```
GET /api/payments/{id}
```

**Ответ включает**:
- Полные данные платежа
- Связанный тип платежа (payment_type) с банковскими реквизитами
- Данные кассира (cashier)
- Все суммы (amount, commission, total)

## Тестирование печати

### Оригинальная квитанция:

1. Откройте страницу создания платежа
2. Создайте тестовый платеж
3. Нажмите кнопку "Печать квитанции"
4. Проверьте предварительный просмотр печати
5. Убедитесь, что квитанция занимает четверть страницы A4

### Дубликат квитанции:

1. Откройте список платежей (`/payments`)
2. Найдите нужный платеж
3. Нажмите кнопку печати (🖨️) в столбце "Действия"
4. Проверьте, что открылось модальное окно с предпросмотром
5. Убедитесь, что видна надпись "ДУБЛИКАТ"
6. Нажмите "Печать дубликата"
7. Проверьте предварительный просмотр печати
8. Убедитесь, что водяной знак "ДУБЛИКАТ" виден на печати

## История изменений

### 2026-01-04
- **Добавлен QR-код с публичной ссылкой** на квитанцию (60×60px)
- **Добавлено поле для подписи** в левой колонке внизу
- **Исправлена проблема с печатью**: строка "Итого" теперь помещается в пределах квитанции
- Уменьшены размеры шрифтов для компактности:
  - Заголовок: 10px → 9px
  - Детали платежа: 10px → 9px
  - ФИО плательщика: 14px → 12px
  - Назначение: 12px → 10px
  - Суммы: 10-12px → 9-10px
- Уменьшены отступы (padding, margin) для более плотной компоновки
- Добавлены улучшенные стили печати с !important для гарантии отображения границ
- Исправлена ширина колонки сумм: `width: 400px/100mm` → `flex: 1`

### 2026-01-03
- Уменьшена высота квитанции вдвое: с ½ A4 (560px/133.5mm) до ¼ A4 (280px/67mm)
- Уменьшены размеры шрифтов для компактного отображения (10-14pt → 7-11pt)
- Уменьшены отступы и gap между элементами
- Уменьшен размер QR-кода с 100px до 60px
- Уменьшен размер водяного знака "ДУБЛИКАТ" с 48pt до 32pt
- Все элементы сохранены, изменены только размеры для компактности
