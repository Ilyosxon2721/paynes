# 🚀 Развертывание Laravel на cPanel с MySQL

## 📋 Содержание
1. [Требования](#требования)
2. [Подготовка к развертыванию](#подготовка-к-развертыванию)
3. [Загрузка файлов на сервер](#загрузка-файлов-на-сервер)
4. [Настройка базы данных MySQL](#настройка-базы-данных-mysql)
5. [Настройка файлов на сервере](#настройка-файлов-на-сервере)
6. [Запуск миграций](#запуск-миграций)
7. [Настройка прав доступа](#настройка-прав-доступа)
8. [Проверка работы](#проверка-работы)
9. [Решение проблем](#решение-проблем)

---

## ✅ Требования

### На хостинге должно быть:
- **PHP 8.3+** (у вас 8.4.16 ✅)
- **MySQL 5.7+** или **MariaDB 10.3+**
- **Composer** (доступен через SSH)
- **SSH доступ** (для выполнения команд)
- Минимум **512 MB RAM**
- Расширения PHP:
  - BCMath
  - Ctype
  - JSON
  - Mbstring
  - OpenSSL
  - PDO
  - Tokenizer
  - XML

### На вашем компьютере:
- Git (для работы с репозиторием)
- FTP/SFTP клиент (FileZilla, Cyberduck) или доступ к File Manager в cPanel

---

## 📦 Подготовка к развертыванию

### 1. Подготовьте проект локально

```bash
cd /Users/ilyosxon/Downloads/admin-kassa-laravel

# Установите зависимости
composer install --optimize-autoloader --no-dev

# Соберите frontend assets
npm install
npm run build

# Очистите кеш разработки
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### 2. Создайте архив для загрузки

```bash
# Исключаем ненужные файлы
zip -r anesi-kassa.zip . \
  -x "node_modules/*" \
  -x ".git/*" \
  -x "storage/logs/*" \
  -x "storage/framework/cache/*" \
  -x "storage/framework/sessions/*" \
  -x "storage/framework/views/*" \
  -x ".env" \
  -x "*.log"
```

Или создайте архив вручную, исключая:
- `node_modules/`
- `.git/`
- `storage/logs/*` (но сохраните папку с `.gitignore`)
- `.env` (этот файл создадим на сервере)

---

## 📤 Загрузка файлов на сервер

### Вариант 1: Через File Manager в cPanel

1. Войдите в **cPanel**
2. Откройте **File Manager**
3. Перейдите в **public_html** (или в нужную директорию)
4. Загрузите архив `anesi-kassa.zip`
5. Кликните правой кнопкой → **Extract** (Распаковать)
6. Удалите архив после распаковки

### Вариант 2: Через SFTP/FTP

1. Подключитесь к серверу через FileZilla/Cyberduck
2. Загрузите все файлы в `public_html/`

### Вариант 3: Через Git (если доступен)

```bash
# Подключитесь по SSH
ssh your-username@your-server.com

# Перейдите в директорию
cd public_html

# Клонируйте репозиторий
git clone your-repository-url .

# Установите зависимости
composer install --optimize-autoloader --no-dev
```

---

## 🗄️ Настройка базы данных MySQL

### 1. Создайте базу данных в cPanel

1. Войдите в **cPanel**
2. Найдите **MySQL Databases**
3. Создайте новую базу данных:
   - Имя: `uz123_kokand` (или ваше)
4. Создайте пользователя:
   - Имя: `uz123_SuperAdmin`
   - Пароль: (создайте надежный пароль)
5. Добавьте пользователя к базе данных с **ALL PRIVILEGES**

### 2. Запишите данные подключения

```
DB_HOST=localhost (или IP: 83.69.139.168)
DB_PORT=3306
DB_DATABASE=uz123_kokand
DB_USERNAME=uz123_SuperAdmin
DB_PASSWORD=ваш_пароль
```

**Важно:** На некоторых хостингах `DB_HOST` может быть:
- `localhost`
- `127.0.0.1`
- IP адрес сервера
- Специфичный хост типа `mysql.yourserver.com`

---

## ⚙️ Настройка файлов на сервере

### 1. Создайте файл .env

Подключитесь по SSH или используйте File Manager:

```bash
# По SSH
cd /home/your-username/public_html
cp .env.production .env
nano .env
```

Или через **File Manager** → скопируйте `.env.production` и переименуйте в `.env`

### 2. Отредактируйте .env файл

Обновите следующие параметры:

```env
APP_NAME="Anesi Kassa"
APP_ENV=production
APP_KEY=base64:HmnpIiIkYWB6qrH/0QcsMixDdYTVZUb1zLJk9ckNkjE=
APP_DEBUG=false
APP_URL=https://your-domain.uz

# ВАЖНО: Укажите ваш реальный домен
APP_URL=https://ваш-домен.uz

# MySQL Database
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=uz123_kokand
DB_USERNAME=uz123_SuperAdmin
DB_PASSWORD=ваш_пароль_от_mysql

# Sanctum (для API)
SANCTUM_STATEFUL_DOMAINS=ваш-домен.uz
SESSION_DOMAIN=.ваш-домен.uz
```

### 3. Настройте Document Root

**Очень важно!** Laravel работает из папки `public/`.

#### Способ 1: Через cPanel (рекомендуется)

1. В cPanel → **Domains** → **Addon Domains** или **Primary Domain**
2. Измените **Document Root** на `/public_html/public`
3. Сохраните

#### Способ 2: Создайте .htaccess в корне

Если не можете изменить Document Root, создайте `.htaccess` в корне `public_html/`:

```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
```

---

## 🚀 Запуск миграций

### По SSH (рекомендуется)

```bash
# Подключитесь по SSH
ssh your-username@your-server.com

# Перейдите в директорию проекта
cd public_html

# Проверьте подключение к БД
php artisan migrate:status

# Запустите миграции
php artisan migrate --force

# Очистите и пересоздайте кеш
php artisan config:cache
php artisan route:cache
php artisan view:cache

# Создайте символическую ссылку для storage
php artisan storage:link
```

### Через Terminal в File Manager

Некоторые cPanel имеют встроенный терминал:
1. File Manager → Terminal (иконка в правом верхнем углу)
2. Выполните те же команды

---

## 🔐 Настройка прав доступа

**Критически важно для безопасности!**

```bash
# По SSH
cd /home/your-username/public_html

# Установите правильного владельца
chown -R your-username:your-username .

# Установите права для папок
find . -type d -exec chmod 755 {} \;

# Установите права для файлов
find . -type f -exec chmod 644 {} \;

# Разрешите запись в storage и cache
chmod -R 775 storage
chmod -R 775 bootstrap/cache

# Защитите .env файл
chmod 600 .env
```

**Важно:** Папки `storage/` и `bootstrap/cache/` должны быть доступны для записи веб-сервером.

---

## 👤 Создание администратора

После успешной миграции создайте первого пользователя:

### Через SSH

```bash
php artisan tinker
```

Затем в Tinker:

```php
$user = App\Models\User::create([
    'login' => 'admin',
    'full_name' => 'Администратор',
    'password' => bcrypt('admin123'),
    'position' => 'admin',
    'status' => 'active'
]);

// Проверьте создание
echo "User created: " . $user->login;

exit
```

### Или создайте Seeder (рекомендуется для production)

```bash
php artisan make:seeder AdminUserSeeder
```

Затем отредактируйте `database/seeders/AdminUserSeeder.php`:

```php
<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use App\Models\User;

class AdminUserSeeder extends Seeder
{
    public function run()
    {
        User::create([
            'login' => 'admin',
            'full_name' => 'Администратор системы',
            'password' => bcrypt('secure_password_here'),
            'position' => 'admin',
            'status' => 'active'
        ]);
    }
}
```

Запустите:

```bash
php artisan db:seed --class=AdminUserSeeder
```

---

## ✅ Проверка работы

### 1. Проверьте доступность сайта

Откройте в браузере: `https://ваш-домен.uz`

Вы должны увидеть главную страницу Laravel или вашу кастомную страницу.

### 2. Проверьте базу данных

```bash
php artisan migrate:status
```

Все миграции должны быть со статусом `Ran`.

### 3. Проверьте логи

```bash
tail -f storage/logs/laravel.log
```

Или через File Manager: `storage/logs/laravel.log`

### 4. Тестовый вход

Если у вас есть страница входа, попробуйте войти с созданными credentials:
- **Логин:** admin
- **Пароль:** admin123 (или тот, что вы указали)

---

## 🛠️ Решение проблем

### Проблема: "500 Internal Server Error"

**Решение:**

1. Проверьте логи:
```bash
tail -50 storage/logs/laravel.log
```

2. Убедитесь, что права настроены правильно:
```bash
chmod -R 775 storage bootstrap/cache
```

3. Очистите кеш:
```bash
php artisan config:clear
php artisan cache:clear
```

### Проблема: "SQLSTATE[HY000] [2002] Connection refused"

**Решение:**

1. Проверьте `.env` файл:
   - `DB_HOST` должен быть `localhost` (не `127.0.0.1`)
   - `DB_DATABASE`, `DB_USERNAME`, `DB_PASSWORD` должны совпадать с данными из cPanel

2. Проверьте подключение:
```bash
php artisan migrate:status
```

3. Если не помогает, попробуйте использовать IP вместо localhost:
```env
DB_HOST=127.0.0.1
```

### Проблема: "The stream or file could not be opened"

**Решение:**

```bash
# Создайте директорию для логов если её нет
mkdir -p storage/logs

# Установите правильные права
chmod -R 775 storage
chown -R your-username:your-username storage
```

### Проблема: "No application encryption key has been set"

**Решение:**

```bash
php artisan key:generate --force
```

### Проблема: Страницы без стилей

**Решение:**

1. Убедитесь, что `APP_URL` в `.env` правильный
2. Пересоберите assets:
```bash
npm run build
```
3. Очистите кеш браузера (Ctrl+Shift+R)

### Проблема: "Class not found"

**Решение:**

```bash
composer dump-autoload --optimize
php artisan config:cache
php artisan route:cache
```

### Проблема: CORS ошибки для API

**Решение:**

Убедитесь в `.env`:
```env
SANCTUM_STATEFUL_DOMAINS=ваш-домен.uz,www.ваш-домен.uz
SESSION_DOMAIN=.ваш-домен.uz
```

---

## 🔄 Обновление проекта

При обновлении кода на сервере:

```bash
# 1. Включите режим обслуживания
php artisan down

# 2. Обновите код (через Git или загрузку файлов)
git pull origin main
# или загрузите новые файлы через FTP

# 3. Обновите зависимости
composer install --no-dev --optimize-autoloader

# 4. Запустите миграции
php artisan migrate --force

# 5. Очистите старый кеш
php artisan config:clear
php artisan route:clear
php artisan view:clear

# 6. Создайте новый кеш
php artisan config:cache
php artisan route:cache
php artisan view:cache

# 7. Пересоберите frontend (если были изменения)
npm run build

# 8. Выключите режим обслуживания
php artisan up
```

---

## 📝 Чеклист развертывания

- [ ] Архив проекта создан и загружен на сервер
- [ ] База данных MySQL создана в cPanel
- [ ] Пользователь БД создан и имеет все привилегии
- [ ] Файл `.env` создан и настроен с правильными данными
- [ ] Document Root указывает на `/public_html/public`
- [ ] Права доступа настроены (775 для storage и bootstrap/cache)
- [ ] Миграции выполнены (`php artisan migrate --force`)
- [ ] Кеш создан (`php artisan config:cache`)
- [ ] Символическая ссылка storage создана (`php artisan storage:link`)
- [ ] Администратор создан через tinker или seeder
- [ ] Сайт открывается в браузере без ошибок
- [ ] Логи проверены на наличие ошибок
- [ ] Вход в систему работает

---

## 🔒 Безопасность Production

### Важные настройки в .env:

```env
APP_ENV=production
APP_DEBUG=false  # НИКОГДА не ставьте true на production!
LOG_LEVEL=error  # Логировать только ошибки
```

### Защита файлов:

```bash
# .env должен быть недоступен извне
chmod 600 .env

# Убедитесь, что .htaccess в public/ защищает .env
```

### Регулярное обслуживание:

```bash
# Очищайте старые логи
php artisan log:clear

# Очищайте старые сессии
php artisan session:gc

# Очищайте кеш если нужно
php artisan cache:clear
```

---

## 📞 Получение помощи

Если возникли проблемы:

1. Проверьте логи: `storage/logs/laravel.log`
2. Проверьте логи веб-сервера в cPanel → Error Log
3. Попробуйте воспроизвести проблему локально
4. Обратитесь в поддержку хостинга если проблема с сервером

---

## 🎯 Полезные команды

```bash
# Проверить версию PHP
php -v

# Проверить установленные расширения PHP
php -m

# Проверить статус миграций
php artisan migrate:status

# Откатить последнюю миграцию
php artisan migrate:rollback

# Очистить все кеши
php artisan optimize:clear

# Создать кеши для production
php artisan optimize

# Проверить маршруты
php artisan route:list

# Посмотреть конфигурацию
php artisan config:show database
```

---

**Готово!** 🎉 Ваше приложение должно работать на production сервере с cPanel и MySQL.
