# 🚀 Деплой через Laravel Forge

Laravel Forge - это официальный инструмент управления серверами от создателя Laravel. Он автоматизирует настройку и деплой, делая процесс максимально простым.

## 📋 Содержание

1. [Подготовка](#1-подготовка)
2. [Создание аккаунта в Forge](#2-создание-аккаунта-в-forge)
3. [Подключение провайдера сервера](#3-подключение-провайдера-сервера)
4. [Создание сервера](#4-создание-сервера)
5. [Подключение GitHub](#5-подключение-github)
6. [Создание сайта](#6-создание-сайта)
7. [Настройка окружения](#7-настройка-окружения)
8. [Настройка деплоя](#8-настройка-деплоя)
9. [SSL сертификат](#9-ssl-сертификат)
10. [Очереди и планировщик](#10-очереди-и-планировщик)
11. [Обновление проекта](#11-обновление-проекта)

---

## 1. Подготовка

### Что вам понадобится:

- ✅ **Аккаунт Laravel Forge**: https://forge.laravel.com (от $12/месяц)
- ✅ **VPS сервер** от одного из провайдеров:
  - DigitalOcean (рекомендуется)
  - Linode
  - Vultr
  - AWS
  - Hetzner
  - UpCloud
  - Или свой собственный сервер
- ✅ **GitHub аккаунт** с доступом к репозиторию
- ✅ **Домен** (для production)

### Стоимость:

- **Laravel Forge**: $12-19/месяц (или $7/месяц для Hobby плана)
- **VPS сервер**: от $4-6/месяц (DigitalOcean, Linode, Vultr)
- **Итого**: ~$16-25/месяц

---

## 2. Создание аккаунта в Forge

### Шаг 1: Регистрация

1. Перейдите на https://forge.laravel.com
2. Нажмите **"Sign Up"**
3. Создайте аккаунт или войдите через GitHub
4. Выберите план:
   - **Hobby**: $7/мес - 1 сервер
   - **Growth**: $12/мес - неограниченно серверов
   - **Business**: $19/мес - команды + расширенные функции

### Шаг 2: Настройка профиля

1. Зайдите в **Account** → **Profile**
2. Заполните информацию
3. Добавьте SSH ключ (опционально)

---

## 3. Подключение провайдера сервера

### Вариант A: DigitalOcean (Рекомендуется)

**Почему DigitalOcean?**
- Простая интеграция с Forge
- Надежность и производительность
- Хорошая цена/качество
- Датацентры по всему миру

**Подключение:**

1. Создайте аккаунт на https://digitalocean.com (если нет)
2. В Forge: **Servers** → **Server Providers** → **DigitalOcean**
3. Нажмите **"Connect DigitalOcean"**
4. Авторизуйтесь и разрешите доступ
5. Forge получит API токен автоматически

### Вариант B: Другие провайдеры (Linode, Vultr, AWS)

Аналогично подключите через **Server Providers** → выберите провайдера → Connect

### Вариант C: Свой сервер (Custom Server)

Если у вас уже есть VPS:

1. **Server Providers** → **Custom VPS**
2. Forge предоставит IP адрес и SSH ключ
3. Добавьте их на ваш сервер

---

## 4. Создание сервера

### Шаг 1: Нажмите "Create Server"

В Forge перейдите: **Servers** → **Create Server**

### Шаг 2: Заполните форму

**Server Provider:**
```
DigitalOcean (или ваш провайдер)
```

**Credentials:**
```
Выберите подключенный аккаунт
```

**Server Name:**
```
paynes-production
```

**Server Size:**
```
Рекомендуется:
- Basic Droplet
- 2 GB RAM / 1 vCPU / 50 GB SSD - $12/месяц
- Или 4 GB RAM / 2 vCPU / 80 GB SSD - $24/месяц (для высоких нагрузок)
```

**Region:**
```
Выберите ближайший к вашим пользователям:
- Frankfurt (для Европы)
- Amsterdam (для Европы)
- New York (для США)
- Singapore (для Азии)
```

**Server Type:**
```
App Server (Laravel, Nginx, MySQL, Redis, etc.)
```

**PHP Version:**
```
PHP 8.3
```

**Database:**
```
MySQL 8.0 (отметьте галочку)
```

**Additional Options:**
Отметьте:
- ✅ **Enable Weekly Backups**
- ✅ **Install Node.js** (для сборки фронтенда)
- ✅ **Install Redis** (для кеша и очередей)

### Шаг 3: Создайте сервер

1. Нажмите **"Create Server"**
2. Forge автоматически:
   - Создаст Droplet
   - Установит Nginx, PHP 8.3, MySQL, Redis, Node.js
   - Настроит firewall
   - Настроит SSH ключи
   - Создаст пользователя `forge`
3. Процесс займет **5-10 минут**

### Шаг 4: Дождитесь завершения

Статус изменится с "Provisioning" на "Active" когда готово.

---

## 5. Подключение GitHub

### Шаг 1: Подключите GitHub к Forge

1. **Account** → **Source Control**
2. Нажмите **"Connect to GitHub"**
3. Авторизуйте Forge в GitHub
4. Разрешите доступ к репозиториям

### Шаг 2: Проверьте подключение

В разделе **Source Control** должен появиться ваш GitHub аккаунт.

---

## 6. Создание сайта

### Шаг 1: Откройте сервер

1. **Servers** → выберите ваш сервер (`paynes-production`)

### Шаг 2: Нажмите "New Site"

Заполните форму:

**Root Domain:**
```
yourdomain.com
```

**Aliases (Optional):**
```
www.yourdomain.com
```

**Project Type:**
```
General PHP / Laravel (выбрано по умолчанию)
```

**Web Directory:**
```
/public
(Оставьте по умолчанию для Laravel)
```

**PHP Version:**
```
PHP 8.3
```

**Create Database:**
```
✅ Отметьте галочку
Database Name: admin_kassa
Database User: admin_kassa_user
```

### Шаг 3: Создайте сайт

Нажмите **"Add Site"**

Forge создаст:
- Nginx конфигурацию
- Директорию проекта `/home/forge/yourdomain.com`
- База данных MySQL
- Пользователя БД

---

## 7. Настройка окружения

### Шаг 1: Откройте сайт

**Servers** → ваш сервер → **Sites** → ваш домен

### Шаг 2: Подключите репозиторий

1. Перейдите на вкладку **"Git Repository"**
2. Заполните:

```
Provider: GitHub
Repository: Ilyosxon2721/paynes
Branch: main
```

3. Нажмите **"Install Repository"**

Forge склонирует проект в `/home/forge/yourdomain.com`

### Шаг 3: Настройте .env

1. Перейдите на вкладку **"Environment"**
2. Отредактируйте переменные окружения:

```ini
APP_NAME="Paynes"
APP_ENV=production
APP_KEY=base64:XXXXX (будет сгенерирован автоматически)
APP_DEBUG=false
APP_URL=https://yourdomain.com
APP_TIMEZONE=Asia/Tashkent

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=admin_kassa
DB_USERNAME=admin_kassa_user
DB_PASSWORD=XXXXX (forge создал автоматически)

BROADCAST_CONNECTION=null
FILESYSTEM_DISK=public
QUEUE_CONNECTION=redis

CACHE_STORE=redis
CACHE_PREFIX=paynes_

SESSION_DRIVER=redis
SESSION_LIFETIME=120

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.yourdomain.com
MAIL_PORT=587
MAIL_USERNAME=noreply@yourdomain.com
MAIL_PASSWORD=your_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@yourdomain.com"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"
```

3. Нажмите **"Save"**

---

## 8. Настройка деплоя

### Шаг 1: Настройте Deploy Script

1. Перейдите на вкладку **"Deployment"**
2. Forge автоматически создал скрипт деплоя
3. **Замените** стандартный скрипт на следующий:

```bash
cd /home/forge/yourdomain.com
git pull origin $FORGE_SITE_BRANCH

$FORGE_COMPOSER install --no-dev --no-interaction --prefer-dist --optimize-autoloader

( flock -w 10 9 || exit 1
    echo 'Restarting FPM...'; sudo -S service $FORGE_PHP_FPM reload ) 9>/tmp/fpmlock

# Установка npm зависимостей и сборка фронтенда
if [ -f package.json ]; then
    npm ci --production
    npm run build
fi

# Миграции
if [ -f artisan ]; then
    $FORGE_PHP artisan migrate --force
fi

# Оптимизация
$FORGE_PHP artisan config:cache
$FORGE_PHP artisan route:cache
$FORGE_PHP artisan view:cache
$FORGE_PHP artisan optimize

# Перезапуск очередей (если настроены)
if [ -d "/etc/supervisor/conf.d" ]; then
    sudo supervisorctl restart all
fi
```

4. Нажмите **"Save"**

### Шаг 2: Первый деплой

1. Нажмите **"Deploy Now"**
2. Forge выполнит:
   - Git pull
   - Composer install
   - npm install и build
   - Миграции
   - Кеширование конфигов
3. Следите за логами в реальном времени

### Шаг 3: Auto Deployment

Включите автоматический деплой при push в GitHub:

1. В секции **"Quick Deploy"**
2. Включите переключатель **"Enable Quick Deploy"**
3. Теперь при каждом push в ветку `main` проект будет обновляться автоматически

---

## 9. SSL сертификат

### Настройка Let's Encrypt (бесплатно)

1. Перейдите на вкладку **"SSL"**
2. Выберите **"LetsEncrypt"**
3. Заполните:

```
Domains: yourdomain.com,www.yourdomain.com
Email: your@email.com
```

4. Нажмите **"Obtain Certificate"**
5. Forge автоматически:
   - Получит SSL сертификат
   - Настроит HTTPS редирект
   - Настроит автообновление (каждые 60 дней)

**Готово!** Ваш сайт теперь доступен по HTTPS.

---

## 10. Очереди и планировщик

### 10.1 Настройка Queue Worker

1. Перейдите на вкладку **"Queue"**
2. Нажмите **"New Worker"**
3. Заполните:

```
Connection: redis
Queue: default
Processes: 2
Max Time: 3600 seconds
Max Jobs: 1000
Max Tries: 3
Sleep: 3 seconds
```

4. Нажмите **"Create Worker"**

Forge автоматически:
- Создаст Supervisor конфигурацию
- Запустит воркеров
- Настроит автоматический перезапуск

### 10.2 Настройка Laravel Scheduler

Laravel Scheduler работает автоматически! Forge уже настроил cron:

```bash
* * * * * cd /home/forge/yourdomain.com && php artisan schedule:run >> /dev/null 2>&1
```

Проверить можно в **Scheduler** → должна быть запись о вашем сайте.

---

## 11. Обновление проекта

### Автоматическое обновление (рекомендуется)

При включенном **Quick Deploy**:
1. Делаете изменения локально
2. Commit и push в GitHub: `git push origin main`
3. Forge автоматически деплоит за 30-60 секунд

### Ручное обновление

Через панель Forge:
1. **Servers** → ваш сервер → **Sites** → ваш домен
2. **Deployment** → **Deploy Now**

Через SSH (если нужно):
```bash
ssh forge@your_server_ip
cd ~/yourdomain.com
php artisan deploy  # Если настроили deploy команду
```

---

## 12. Полезные функции Forge

### Мониторинг

**Servers** → ваш сервер → **Monitoring**
- CPU использование
- Память
- Дисковое пространство
- Сетевой трафик

### Бэкапы

**Servers** → ваш сервер → **Backups**
- Настройте автоматические бэкапы БД
- Храните в S3, DigitalOcean Spaces и т.д.

### Логи

**Sites** → ваш домен → **Logs**
- Laravel logs
- Nginx access/error logs

### Безопасность

**Servers** → ваш сервер → **Security**
- Firewall правила (автоматически настроены)
- SSH ключи
- Fail2Ban (защита от brute-force)

### Команды

**Sites** → ваш домен → **Commands**
- Выполняйте artisan команды через UI:
  ```
  php artisan cache:clear
  php artisan migrate
  php artisan queue:restart
  ```

---

## 13. Troubleshooting

### Проблема: Деплой падает с ошибкой

**Решение:**
1. Проверьте логи деплоя в Forge
2. Проверьте Laravel логи: **Sites** → **Logs** → **Laravel**
3. SSH на сервер и проверьте вручную:
```bash
ssh forge@your_server_ip
cd ~/yourdomain.com
php artisan migrate --force
```

### Проблема: 500 Internal Server Error

**Решение:**
1. Проверьте права доступа:
```bash
ssh forge@your_server_ip
cd ~/yourdomain.com
sudo chown -R forge:forge .
chmod -R 775 storage bootstrap/cache
```

2. Очистите кеши:
```bash
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

### Проблема: npm build падает

**Решение:**
Увеличьте память Node.js в deploy скрипте:
```bash
export NODE_OPTIONS="--max-old-space-size=2048"
npm ci --production
npm run build
```

---

## 14. Стоимость и оптимизация

### Текущие расходы:

```
Laravel Forge: $12/месяц (Growth план)
DigitalOcean VPS (2GB): $12/месяц
Домен: ~$10-15/год
SSL: $0 (Let's Encrypt бесплатно)
--------------------------------
Итого: ~$25/месяц + $12/год
```

### Способы экономии:

1. **Hobby план Forge**: $7/мес (для 1 сервера)
2. **Меньший Droplet**: $6/мес (1GB RAM) - для небольшого трафика
3. **Годовая оплата**: Скидки у DigitalOcean и Forge

---

## 15. Чеклист готовности к production

- [ ] Forge аккаунт создан
- [ ] Провайдер сервера подключен (DigitalOcean)
- [ ] Сервер создан и активен
- [ ] GitHub подключен к Forge
- [ ] Сайт создан в Forge
- [ ] Репозиторий подключен и склонирован
- [ ] .env правильно настроен
- [ ] База данных создана
- [ ] Первый деплой выполнен успешно
- [ ] SSL сертификат установлен (HTTPS работает)
- [ ] Queue worker настроен и работает
- [ ] Laravel Scheduler активен
- [ ] Quick Deploy включен
- [ ] Домен направлен на IP сервера
- [ ] Сайт открывается в браузере
- [ ] Протестированы основные функции

---

## 🎉 Поздравляем!

Ваш Laravel проект теперь работает на production через Forge!

### Преимущества Forge:

✅ **Простота** - настройка за 15 минут вместо нескольких часов
✅ **Автоматизация** - деплой одной кнопкой
✅ **Безопасность** - автоматические обновления, firewall, SSL
✅ **Мониторинг** - следите за производительностью
✅ **Масштабирование** - легко добавить новые серверы
✅ **Поддержка** - от создателя Laravel

### Полезные ссылки:

- Forge Документация: https://forge.laravel.com/docs
- Forge Status: https://status.forge.laravel.com
- Laravel Docs: https://laravel.com/docs
- Forge Discord: https://discord.gg/laravel

---

**Готово! Наслаждайтесь простотой деплоя с Laravel Forge! 🚀**
