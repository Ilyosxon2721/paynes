# Contributing to Anesi Kassa

Спасибо за ваш интерес к улучшению Anesi Kassa! Этот документ содержит рекомендации по внесению вклада в проект.

## Содержание

- [Начало работы](#начало-работы)
- [Процесс разработки](#процесс-разработки)
- [Стиль кода](#стиль-кода)
- [Тестирование](#тестирование)
- [Pull Requests](#pull-requests)

## Начало работы

### Требования

- PHP 8.3+
- Composer
- Node.js 18+
- MySQL 8.0+ или SQLite
- Git

### Локальная установка

```bash
# Клонировать репозиторий
git clone https://github.com/your-org/anesi-kassa.git
cd anesi-kassa

# Установить зависимости
composer install
npm install

# Настроить окружение
cp .env.example .env
php artisan key:generate

# Создать базу данных
touch database/database.sqlite

# Запустить миграции
php artisan migrate
php artisan db:seed

# Собрать фронтенд
npm run dev
```

## Процесс разработки

### Ветки

- `main` - production-ready код
- `develop` - основная ветка разработки
- `feature/*` - новые функции
- `bugfix/*` - исправления ошибок
- `hotfix/*` - срочные исправления для production

### Рабочий процесс

1. Создайте issue для обсуждения изменений
2. Создайте ветку от `develop`:
   ```bash
   git checkout develop
   git pull origin develop
   git checkout -b feature/your-feature-name
   ```
3. Внесите изменения
4. Напишите тесты
5. Убедитесь, что все тесты проходят
6. Создайте Pull Request

## Стиль кода

### PHP

Мы используем Laravel Pint для автоматического форматирования:

```bash
# Проверить код
vendor/bin/pint --test

# Исправить автоматически
vendor/bin/pint
```

**Основные правила:**

- PSR-12 стандарт
- Строгая типизация (`declare(strict_types=1)`)
- PHPDoc комментарии для всех публичных методов
- Осмысленные имена переменных и методов

### JavaScript/Vue.js

```bash
# Проверить код
npm run lint

# Исправить автоматически
npm run lint:fix
```

**Основные правила:**

- ES6+ синтаксис
- Composition API для Vue компонентов
- Camel case для переменных и функций

### Именование

**PHP:**
- Классы: `PascalCase`
- Методы: `camelCase`
- Переменные: `$camelCase`
- Константы: `SCREAMING_SNAKE_CASE`

**Vue.js:**
- Компоненты: `PascalCase.vue`
- Props: `camelCase`
- Events: `kebab-case`

## Тестирование

### Запуск тестов

```bash
# Все тесты
php artisan test

# Конкретный тест
php artisan test --filter=PaymentTest

# С покрытием
php artisan test --coverage
```

### Написание тестов

Каждая новая функция должна включать тесты:

**Feature тесты** (для API endpoints):
```php
public function test_can_create_payment(): void
{
    $response = $this->postJson('/api/payments', $data);
    $response->assertStatus(201);
}
```

**Unit тесты** (для логики):
```php
public function test_calculates_commission_correctly(): void
{
    $commission = $paymentType->calculateCommission(1000);
    $this->assertEquals(25, $commission);
}
```

## Pull Requests

### Контрольный список

Перед созданием PR убедитесь, что:

- [ ] Код соответствует стилю проекта (Pint пройден)
- [ ] Все тесты проходят
- [ ] Новый код покрыт тестами
- [ ] Документация обновлена (если нужно)
- [ ] Commit messages осмысленные
- [ ] Нет конфликтов с `develop`

### Описание PR

**Шаблон:**

```markdown
## Описание
Краткое описание изменений

## Тип изменения
- [ ] Новая функция
- [ ] Исправление ошибки
- [ ] Улучшение производительности
- [ ] Рефакторинг
- [ ] Документация

## Как тестировать
1. Шаги для воспроизведения
2. Ожидаемый результат

## Скриншоты (если применимо)

## Checklist
- [ ] Тесты написаны и проходят
- [ ] Документация обновлена
- [ ] Code style проверен
```

### Процесс ревью

1. Автоматические проверки (CI/CD)
2. Code review от минимум 1 разработчика
3. Тестирование функциональности
4. Merge в `develop`

## Commit Messages

Используйте conventional commits:

```
type(scope): subject

body

footer
```

**Типы:**
- `feat`: новая функция
- `fix`: исправление ошибки
- `docs`: документация
- `style`: форматирование
- `refactor`: рефакторинг
- `test`: тесты
- `chore`: рутинные задачи

**Примеры:**
```
feat(payments): add commission calculation
fix(auth): resolve token expiration issue
docs(api): update payment endpoints documentation
```

## Вопросы?

Если у вас есть вопросы, создайте issue или свяжитесь с командой.

Спасибо за ваш вклад! 🚀
