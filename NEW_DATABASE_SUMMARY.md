# Новая структура БД - Итоговая сводка

**Дата:** 2026-01-02
**Статус:** ✅ **ГОТОВО**

---

## ✅ Что сделано

### 1. Создана новая структура БД

Старая БД **uz123_kokand** была полностью очищена и пересоздана с новой, улучшенной структурой.

**Всего таблиц:** 23
**Размер:** 720 KB (пустая БД)

---

## 📊 Структура таблиц

### Основные таблицы приложения:

#### 1. **`users`** - Пользователи (кассиры, админы)
Поля:
- `id` (PK)
- `login` (unique)
- `full_name`
- `added_by` ✨ (новое - кто добавил)
- `password` (bcrypt)
- `position` (enum: admin, cashier)
- `status` (enum: active, blocked)
- **`branch`** ✨ (новое - филиал кассира)
- **`reward_percentage`** ✨ (новое - процент вознаграждения)
- `remember_token`
- `created_at`, `updated_at`

#### 2. **`agents`** ✨ **НОВАЯ ТАБЛИЦА**
Поля:
- `id` (PK)
- `login` (unique)
- `password` (bcrypt)
- `full_name`
- `added_by`
- `position`
- **`reward_percentage`** - процент агента
- **`branch`** - филиал агента
- `status` (enum: active, inactive)
- **`phone`** - телефон агента
- `created_at`, `updated_at`, `deleted_at`

#### 3. **`payments`** - Платежи
Поля:
- `id` (PK)
- `list_number`
- `random_number`
- `date`, `time`
- `payment_type_id` (FK → payment_types)
- `payer_name` - плательщик
- `purpose` - назначение платежа
- `amount` - сумма
- `commission` - комиссия
- `total` - итого
- `payment_method` (enum: cash, card)
- `payment_system` ✨ (новое - UzCard, Humo, etc)
- `currency` (enum: UZS, USD)
- `status` (enum: pending, confirmed, deleted, processed)
- `cashier_id` (FK → users)
- `agent_id` ✨ (новое - FK → agents, nullable)
- **`city`** ✨ (новое - город)
- **`region`** ✨ (новое - регион)
- **`cash_back`** ✨ (новое - кэшбэк клиенту)
- `created_at`, `updated_at`, `deleted_at`

#### 4. **`payment_types`** - Типы платежей
Поля:
- `id` (PK)
- `name` (unique)
- `commission_percentage`
- `description`
- `is_active`
- `created_at`, `updated_at`

#### 5. **`exchanges`** - Обмен валюты
Поля:
- `id` (PK)
- `date`, `time`
- `from_currency` (enum: UZS, USD)
- `to_currency` (enum: UZS, USD)
- `from_amount`
- `to_amount`
- `rate` - курс обмена
- `type` (enum: buy, sell)
- `cashier_id` (FK → users)
- `created_at`, `updated_at`

#### 6. **`credits`** - Кредиты
Поля:
- `id` (PK)
- `date`, `time`
- `recipient` - получатель
- **`account_number`** ✨ (xr - номер счета)
- **`branch`** ✨ (филиал)
- **`debit`** ✨ (дебет)
- **`credit`** ✨ (кредит)
- **`confirmed_by`** ✨ (кто подтвердил)
- `status` (enum: pending, confirmed, deleted)
- `cashier_id` (FK → users)
- `created_at`, `updated_at`, `deleted_at`

#### 7. **`incashes`** - Инкассация
Поля:
- `id` (PK)
- `date`, `time`
- `account_number` - номер счета
- `amount` - сумма
- `cashier_id` (FK → users)
- `type` (enum: income, expense)
- `status` (enum: pending, processed)
- `created_at`, `updated_at`

#### 8. **`rates`** - Курсы валют
Поля:
- `id` (PK)
- `currency_from` (enum: UZS, USD)
- `currency_to` (enum: UZS, USD)
- `buy_rate` - курс покупки
- `sell_rate` - курс продажи
- `is_active` - активный курс
- `created_at`, `updated_at`

#### 9. **`fines`** ✨ **НОВАЯ ТАБЛИЦА**
Поля:
- `id` (PK)
- `date`, `time`
- `user_id` (FK → users)
- `amount` - сумма штрафа
- `reason` - причина штрафа
- `status` (enum: pending, paid, cancelled)
- `created_at`, `updated_at`, `deleted_at`

---

### Системные таблицы Laravel:

- **`migrations`** - история миграций
- **`password_reset_tokens`** - токены сброса пароля
- **`sessions`** - сессии пользователей
- **`personal_access_tokens`** - API токены (Sanctum)
- **`cache`**, **`cache_locks`** - кэш
- **`jobs`**, **`job_batches`**, **`failed_jobs`** - очереди задач

### Таблицы прав доступа (Spatie Permission):

- **`permissions`** - разрешения
- **`roles`** - роли
- **`model_has_permissions`** - связь модель-разрешение
- **`model_has_roles`** - связь модель-роль
- **`role_has_permissions`** - связь роль-разрешение

---

## 🆕 Новые возможности БД

### 1. Поддержка агентов
- Отдельная таблица `agents`
- Связь агент-платёж через `agent_id` в payments
- Процент вознаграждения агента

### 2. География платежей
- Поля `city` и `region` в payments
- Возможность анализа по регионам

### 3. Кэшбэк
- Поле `cash_back` в payments
- Учет возврата средств клиентам

### 4. Штрафы
- Новая таблица `fines`
- Учет штрафов для сотрудников

### 5. Детальная информация о кредитах
- Номер счета (`account_number`)
- Дебет и кредит
- Филиал и подтверждающий

### 6. Филиалы
- Поле `branch` в users и agents
- Отчеты по филиалам

### 7. Платёжные системы
- Поле `payment_system` (UzCard, Humo, etc.)
- Детальная аналитика по системам

---

## 🔑 Ключевые отличия от старой БД

### Старая БД → Новая БД:

| Аспект | Старая БД | Новая БД |
|--------|-----------|----------|
| **Таблиц** | 14 | 23 |
| **Пароли** | MD5 | bcrypt (Laravel) |
| **Timestamps** | `data`/`clock` | `created_at`/`updated_at` |
| **Soft Deletes** | Нет | Есть (payments, credits, agents, fines) |
| **FK Constraints** | Частично | Полностью (cascadeOnDelete) |
| **Naming** | Русские транслиты | Английский |
| **Status** | Текст | ENUM |
| **Агенты** | В users | Отдельная таблица |
| **Штрафы** | Отдельная таблица | Отдельная таблица |
| **Права доступа** | Нет | Spatie Permission |
| **API Auth** | Нет | Sanctum |

---

## 📝 Маппинг полей (старая → новая)

### Payments:
```
data         → date
clock        → time
random       → random_number
list         → list_number
name         → payer_name
appoint      → purpose
summa        → amount
komissiya    → commission
jami         → total
type         → payment_type_id (найти по имени)
FIO          → cashier_id (найти по имени)
valyuta      → currency ("сум" → "UZS")
status       → status ("Подтвержден" → "confirmed")
typepay      → payment_method ("Наличные" → "cash")
+ city       → city (новое)
+ region     → region (новое)
+ cashBack   → cash_back (новое)
+ agent_id   → agent_id (новое)
+ payedSysteam → payment_system (новое)
```

### Users:
```
login    → login
pass     → password (bcrypt!)
name     → full_name
position → position ("Супер Админ" → "admin")
status   → status ("Активный" → "active")
+ branch → branch (было в старой, сохранено)
+ reward → reward_percentage (было в старой, сохранено)
+ added  → added_by (новое)
```

### Exchange:
```
USD      → from_amount (если покупка) / to_amount (если продажа)
UZS      → to_amount (если покупка) / from_amount (если продажа)
type     → type ("Покупка" → "buy")
rate     → rate
cashier  → cashier_id (найти по ФИО)
```

---

## ✅ Старая БД - что с ней?

Старая БД **uz123_kokand** была **полностью удалена** и заменена новой структурой.

**История платежей (112,000+ записей) НЕ мигрирована**, как и было согласовано.

Для доступа к историческим данным:
- Можно использовать SQL dump: `docs/бд/uz123_kokand (2).sql`
- Или восстановить старую БД во временную для справки

---

## 🚀 Следующие шаги

1. ✅ Создать seed'ы для базовых данных:
   - Типы платежей (payment_types)
   - Курсы валют (rates)
   - Роли и разрешения (roles, permissions)
   - Админ-пользователь

2. ✅ Обновить модели Payment, Credit, Exchange, Incash:
   - Добавить новые поля в `$fillable`
   - Обновить relationships (agent)

3. ✅ Обновить контроллеры и API:
   - Поддержка новых полей (city, region, cash_back, agent_id)
   - Валидация

4. ✅ Обновить Vue.js интерфейс:
   - Формы с новыми полями
   - Фильтры по филиалам, агентам

---

## 📦 Созданные файлы

### Миграции:
- `2026_01_02_173611_create_agents_table.php` ✨
- `2026_01_02_173654_add_old_db_fields_to_payments_table.php` ✨
- `2026_01_02_173854_create_fines_table.php` ✨
- Обновлена: `0001_01_01_000000_create_users_table.php`
- Обновлены: все базовые миграции credits, payments, etc.

### Модели:
- `app/Models/Agent.php` ✨
- `app/Models/Fine.php` ✨

### Документация:
- `MIGRATION_PLAN.md` - план миграции данных (не использован)
- `DATABASE_COMPARISON.md` - подробное сравнение БД
- `NEW_DATABASE_SUMMARY.md` - текущий файл
- `drop_old_tables.sql` - скрипт очистки БД

---

## 🎯 Итог

✅ **Новая БД успешно создана!**

**Преимущества новой структуры:**

1. ✨ Современный Laravel-стандарт
2. ✨ Поддержка агентов и штрафов
3. ✨ География платежей (city, region)
4. ✨ Кэшбэк для клиентов
5. ✨ Платёжные системы (UzCard, Humo)
6. ✨ Филиалы для users и agents
7. ✨ Детальная информация о кредитах
8. ✨ Soft Deletes для всех важных таблиц
9. ✨ FK constraints с cascade
10. ✨ Права доступа (Spatie Permission)
11. ✨ API Authentication (Sanctum)

**Готово к разработке!** 🚀

---

**Версия:** 1.0
**Дата:** 2026-01-02
